// === FIREBASE CONFIGURATION ===
//
// To enable shared checklist sync across all travelers:
//
// 1. Go to https://console.firebase.google.com/
// 2. Click "Create a project" (free tier is plenty)
// 3. Go to Project Settings > General > Your apps > Add web app
// 4. Copy the config object below
// 5. Go to Realtime Database > Create Database > Start in test mode
// 6. Replace the placeholder values below with your real config
//
// Without this, the checklist saves locally in each browser (localStorage).

window.FIREBASE_CONFIG = {
  // apiKey: "YOUR_API_KEY",
  // authDomain: "YOUR_PROJECT.firebaseapp.com",
  // databaseURL: "https://YOUR_PROJECT-default-rtdb.firebaseio.com",
  // projectId: "YOUR_PROJECT",
  // storageBucket: "YOUR_PROJECT.appspot.com",
  // messagingSenderId: "YOUR_SENDER_ID",
  // appId: "YOUR_APP_ID"
};
